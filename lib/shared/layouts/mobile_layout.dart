import 'package:flutter/widgets.dart';

class MobileLayout extends StatelessWidget {
	const MobileLayout({required this.child, super.key});

	final Widget child;

	@override
	Widget build(BuildContext context) => child;
}
