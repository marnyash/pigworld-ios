import 'package:flutter/material.dart';
import '../../../../shared/widgets/module_page.dart';

class FarmManagementPage extends StatelessWidget { const FarmManagementPage({super.key}); @override Widget build(BuildContext context) => const ModulePage(title: 'Farm management', description: 'Manage farms, members, and active farm settings.', icon: Icons.agriculture_outlined); }
