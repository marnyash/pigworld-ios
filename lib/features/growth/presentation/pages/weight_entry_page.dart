import 'package:flutter/material.dart';
import '../../../../shared/widgets/module_page.dart';

class WeightEntryPage extends StatelessWidget { const WeightEntryPage({super.key}); @override Widget build(BuildContext context) => const ModulePage(title: 'Weight entry', description: 'Capture an animal weight measurement.', icon: Icons.monitor_weight_outlined); }
