import 'package:flutter/material.dart';
import '../../../../shared/widgets/module_page.dart';

class HealthPage extends StatelessWidget { const HealthPage({super.key}); @override Widget build(BuildContext context) => const ModulePage(title: 'Health', description: 'Keep health events, treatments, and mortality records organized.', icon: Icons.health_and_safety_outlined); }
