import 'package:flutter/material.dart';
import '../../../../shared/widgets/module_page.dart';

class PregnancyPage extends StatelessWidget { const PregnancyPage({super.key}); @override Widget build(BuildContext context) => const ModulePage(title: 'Pregnancy', description: 'Monitor pregnancy status and due dates.', icon: Icons.pets_outlined); }
