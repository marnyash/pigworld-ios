import 'package:flutter/material.dart';
import '../../../../shared/widgets/module_page.dart';

class MatingPage extends StatelessWidget { const MatingPage({super.key}); @override Widget build(BuildContext context) => const ModulePage(title: 'Mating', description: 'Record and review mating activity.', icon: Icons.favorite_outline); }
